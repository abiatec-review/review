const initialState = {
  searchQuery: '',
  characterId: '',
  characters: [],
  haveError: false,
  pagesInfo: [],
};

const Characters = (action: any, state = initialState) => {
  switch (action.type) {
    default: return state;
  }
};

export default Characters;