import * as React from 'react';
import {TextInput} from 'react-native';

interface CharacterItemProps {}

const CharacterItem = () => {
    return <TextInput>Character</TextInput>;
};

export default CharacterItem;
